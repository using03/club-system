# AGENTS.md

## Cursor Cloud specific instructions

This is the `club-system` repository. As of initial setup, it is a freshly initialized Node.js-oriented repository (indicated by the `.gitignore` template) with no application code or dependencies yet.

### Environment

- **Node.js**: v22 (managed via nvm)
- **npm**: v10
- **pnpm**: v10 (also available)

### Development notes

- No `package.json`, application code, tests, or build configuration exist yet.
- The `.gitignore` covers Node.js, Next.js, Nuxt.js, Vite, SvelteKit, and other common JS frameworks.
- Once application code is added, update this file with service startup instructions, testing commands, and any non-obvious caveats.
